# 報告：伝達核 4人版 4p-v0.3.2 差分 — ルール説明補強とアンカー減光表示

**対象指示書:** 指示書：伝達核 4人版 4p-v0.3.2 差分 — ルール説明補強とアンカー減光表示 — v1（2026-07-18 design chat発行）
**前提VER:** `4p-v0.3.1`（コミット`bcecc23`）
**成果物VER:** `4p-v0.3.2`
**ゲームの挙動・核ルール・凍結定義・数値：無変更**（§2のみ表示追加。他は文言のみ）

---

## §1-a 実装の実挙動・実定数との突合結果（最重要）

`hostSettleDrain()`を精査した結果、指示書が例示した文言「(1) 潜水士が考えている時間（カード選択・ベットの間）は
自チームの酸素が少しずつ減ります」は**実装と一致しない**ことが分かった（v0.3.1のときの「酸素を奪う」表現と同種の、
design chat側の見立てと実装の差異）。実際の挙動は次のとおり非対称：

- **カード選択フェーズ**：`teamsAffected`は常に`['A','B']`（両ダイバーが同時に考えているため）。**両チーム同時に**減る。
- **ベット応酬フェーズ**：`teamsAffected = [g.betting.toActTeam]`。**その時点で手番があるチームだけ**減る（手番でない側は減らない）。
- **ヒントフェーズ**：`hintDrainToggle==='equal'`の場合のみ`['A','B']`（両チーム同時・同速度）。`'off'`なら減らない。
- 上記いずれの条件にも該当しない場合（例：`drainSpeed==='off'`、または`resolved`等の判定・演出フェーズ）は`rateMs`が
  設定されず、関数の先頭付近で早期returnする＝**減らない**。

この正確な非対称性に基づいてルール文を執筆した（指示書の例示文言をそのまま採用せず、実装に忠実な表現へ修正）。

---

## 追記・修正後のルール説明 全文（再レビュー用）

### 概要（常時表示）
> 2チーム対抗の伝達ゲームです。各チームは観測員1名と潜水士1名に分かれ、観測員だけが自チームの「軸」（動物の並び順の基準）を知っています。**各チームは別々の軸を持ちます（同じ軸を共有しているわけではありません）。**
> 潜水士は、観測員がテキストで送るヒント（無線）だけを頼りに、8匹の動物図鑑から1匹を選んで対戦相手と勝負します。
> 選んだ動物の順位を、それぞれ自分のチームの軸で比較し、勝った方がそのラウンドのポット（酸素）を得点にします。どちらかのチームの酸素が先に0になるとそのチームは得点に関わらず即座に敗北し、そうでなければ8ラウンド終了時に得点が多い方の勝ちです。

### 📡 観測員向けタブ
> **観測員のあなたがすること**
> - あなたは自チームの軸（体重・速さ・寿命・危険度・かわいさ・人気・五十音・群れ、のいずれか1つ。相手チームは別の軸）を知っています。
> - ラウンド1〜4の各ラウンドでヒントを1通送れます。内容も、どこまで露骨に言うかも毎回自由です。鋭いヒントは味方に確実に届きますが、敵チームにも同じだけ聞こえます（NPCチームの観測員は、決まった段階〔遠回し→遠回し→中間→直接的〕で送ります）。
> - **R5以降はヒントなしで直接カード選択に進みます。**（前回、この一文がNPC専用の括弧の中に埋もれており、全員に適用される規則だと分かりにくかったため、独立した項目に分離）
> - ヒントは40字以内の自由なテキストです。相手チームの観測員も同時に（並行して）ヒントを書いており、両方が送信ロックするか60秒経過すると同時に公開されます。
> - 送信ロック後は取り消せません。ロック前に確認画面が一度出ます。
> - 締切までロックしなかった場合は「無線沈黙」として全員に表示されます。
> - 軸の名前そのものを言うことは自由です（禁止されていません）。ただし、数字（算用数字・漢数字含む）や「1位」「最大」「一番」等の順位を直接示す語句は自動フィルタでブロックされます。
> - ヒントで嘘をつくことは禁止です（自チームの軸に照らして正直であること。曖昧さ・遠回しは自由）。試合終了時に両チームの軸と全ヒントが公開され、全員で事後検証できます。
> - 潜水士は入力できません。あなたの送信だけが頼りです。

### 🤿 潜水士向けタブ
> **潜水士のあなたがすること**
> - あなたは軸を知りません。チームメイトの観測員が送るヒントの無線だけを頼りに動物を選びます。
> - ヒントフェーズが終わると、8匹の動物図鑑からまだ使っていない1匹を選んで伏せて提出します。
> - あなたには「アンカー」という消去情報が2つ配られています。これは、あなたのチームの軸でも、相手チームの軸でもないと確定している軸です（二重の消去情報）。自チームのヒントの解読の裏取りにも、相手チームの軸の絞り込みにも使えます。
> - **動物を選んでいる間は両チームとも、ベット応酬であなたの手番を考えている間は自チームだけ、少しずつ酸素を消費します（設定がオフの場合は消費しません）。**
> - 両チームが動物を提出すると、両陣営それぞれの軸での順位を比較して勝敗が決まります。同じ動物を出した場合、または異なる動物でも両陣営の軸上の順位が同じ数値になった場合は、引き分けです。
> - 勝敗が決まる前にベット応酬（チェック／ベット／コール／レイズ／フォールド）があります。確定前に一度確認画面が出ます。

### 📖 用語集タブ
> - 軸（じく）：動物を並び替える基準。8種類のうち、各チームに1つずつ、別々に割り当てられます。
> - アンカー：潜水士だけが知る、自チームの軸でも相手チームの軸でもないと確定している消去情報（二重の消去情報）。2つ配られます。
> - **ドレイン：時間経過による酸素の消費。カード選択中は両チームの潜水士が同時に考えているため両チームの酸素が同時に減り、ベット応酬中はその時点で手番があるチームだけ酸素が減ります。ヒントフェーズ中は、設定が「等額」なら両チームの酸素が同時に・同じ速さで微かに減ります（アンビエントドレイン。観測員の書く速さでは差がつきません）。いずれも「オフ」に設定した場面では減りません。判定や演出などシステムの処理中は減りません（速度は部屋作成時に設定できます）。**
> - 酸素：チームの残りチップ。ベットで賭けた酸素はポットに入り、そのラウンドの勝者チームの得点に加算されます（酸素そのものが相手に移るわけではありません）。酸素が先に0になったチームは即座に敗北します。
> - 得点：ポットを獲得した回数の積み上げ。酸素が両チームとも残っている（または同時に尽きた）まま8ラウンドを終えたときは、得点が多い方の勝ちです。
> - **アンティ：各ラウンド開始時に両チームから自動で徴収される参加料（酸素1）。ポットに入ります。**
> - **ポット：そのラウンドに賭けられた酸素の合計。勝ったチームの得点になります。**
> - **チェック：追加で賭けずに相手に手番を渡すこと。**
> - **ベット：酸素を賭けること（1・2・3・5から選択）。**
> - **コール：相手のベットと同額を出して勝負を受けること。**
> - **レイズ：相手のベットにさらに上乗せすること（1ラウンドに3回まで）。**
> - **ショーダウン：両チームのカードを突き合わせて勝敗を決める瞬間。それぞれ自分のチームの軸での順位で比べます。**
> - 無線：観測員から送られるヒントのテキストのこと。
> - ロック：ヒントの送信を確定すること。ロック後は編集・取消不可。
> - フォールド：ベット応酬を降りること。相手チームがそのラウンドの勝ちになります。

（太字部分が本差分での追記。アンティ・ベット額・レイズ上限の数値は`ANTE_PER_TEAM`/`BET_OPTIONS`/`MAX_RAISES`から
v0.3.1と同じ単一ソース方式で動的表示）

### 部屋作成画面への注記（新規）
> 決定フェーズのドレイン設定の下：「カード選択中は両チーム同時に、ベット応酬中はその時点の手番チームだけ、時間経過で酸素が少しずつ減ります（オフなら減りません）。」
> ヒントフェーズのアンビエントドレイン設定の下：「『等額』なら、ヒントを書いている間は両チームの酸素が同時に・同じ速さで微かに減ります（観測員の書く速さでは差がつきません）。」

---

## §2 アンカー減光表示（実装済み）

`renderAxisReferenceTable(anchorAxes)`に`anchorAxes`引数を追加。呼び出し元（`renderGameScreen`）は
`role==='diver' && ST.myPrivate && ST.myPrivate.anchors`のときだけ、**本人の`ST.myPrivate.anchors`**
（自分のクライアントが購読する自分のprivateノード由来のデータのみ）を渡す。観測員の画面・相手チームの画面には
常に空配列が渡り、減光表示は一切発生しない。

該当2軸の行に`.anchor-dim`（`opacity:0.45`）クラスと「アンカー」バッジ（`.anchor-badge`）を付与。

**コード内コメントとして残した裁定根拠：**
```
// anchorAxes: 呼び出し側が本人のprivateデータ（ST.myPrivate.anchors）からのみ渡す。
// アンカーは推論ゼロの配布済み情報の表示（判断の補助）であり、ショーダウン結果等からの
// 導出済み除外は表示しない（publicConstraints表示撤去と同じ裁定。§0-4対症療法カウンタ対象・
// 今後の減光/強調の追加はdesign chat承認制。4p-v0.3.2 §2）。
```

秘匿境界の確認：`renderAxisReferenceTable`への引数はクライアント側で`ST.myPrivate.anchors`のみを参照しており、
`ST.allPrivate`（ホストが中継のために購読する全員のprivateデータ）や他座席の`ST.seats[i]`を経由する経路は無い
（コードレビュー＋静的テストで確認）。ルールテストの秘匿境界（`private/{uid}`は本人とホストのみ読み書き可）自体は
無変更で、この表示機能もその制約の内側で動作する。

---

## テスト結果

- 静的テスト：**29/29 全通過**（既存25件＋本差分で追加した4件：用語集の新規項目、概要の2軸明示、R5文の分離、
  アンカー減光の実装とデータ出所の検証）
- ルールテスト：**11/11 全通過**（ルール自体は無変更のため回帰なし）
- 実機確認（Firebase Emulator + Playwright）：4人が着席しゲーム開始後、潜水士（seat1）の画面で
  軸参照表のアンカー2行が減光＋バッジ表示されることを確認。同チーム観測員（seat0）・敵チーム双方の画面では
  減光表示が0件であることを確認。ルール説明モーダルの用語集タブに、追加した8用語（ドレイン・アンティ・ポット・
  チェック・ベット・コール・レイズ・ショーダウン）が実際にDOM上へ表示されることを確認した
  （`ALL 4P-V0.3.2 ANCHOR-DIM TESTS PASSED`）。

---

## push・bundle結果

- コミットハッシュ：`a89b2b4817f318a4b3c52698162aba4a971df6e1`
- `git push -u origin claude/new-session-sgqt4i`：Bashツール許可層で拒否（本セッション恒常のポリシー）
- フォールバック：全履歴入りbundleを生成
  - ファイル名：`transmit4p-v0_3_2-full.bundle`
  - `git bundle verify`結果：
    ```
    The bundle contains this ref:
    a89b2b4817f318a4b3c52698162aba4a971df6e1 refs/heads/claude/new-session-sgqt4i
    The bundle records a complete history.
    The bundle uses this hash algorithm: sha1
    ```
  - 適用手順：
    ```
    git fetch transmit4p-v0_3_2-full.bundle claude/new-session-sgqt4i:claude/new-session-sgqt4i
    git checkout claude/new-session-sgqt4i
    git push -u origin claude/new-session-sgqt4i
    ```
